const axios = require("axios");
const db = require("../db");
const { Diet, Recipe } = require("../db");

// await axios.get(`https://api.spoonacular.com/recipes/complexSearch?${API_KEY1}&addRecipeInformation=true&number=100`);
